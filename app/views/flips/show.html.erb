<h1>SOLVE THIS FUCKING PROBLEM</h1>

<p>
  Name: <%= @flip.class %>
  Description: <%= @flip.description %> </br>
  Sample Input: <code><%= display_output(@flip.input) %></code> </br>
  Sample Output: <code><%= @flip.valid_output %> </code></br>
</p>

<br/>
<p>
  <% if current_team.turn_attempts > 0 %>
    <h2>
      FAIL! <br/>
      Attempts: <%= current_team.turn_attempts %></br>
      Output: <code><%= display_output(@flip.run_user_code(params[:solution]))%></code>
    </h2>
  <% end %>
<code>
def solve(input)<br/>
<%= form_for(@flip, :url => solve_flip_url)  do |f| %>
  <%= text_area_tag :solution, params[:solution], :size => "200x10" %></br>
end                                                            
</br></br>
  <%= submit_tag 'Run my stupid code' %>
<% end %>
</code>
</p>
<%# do something with @flip %>
